#include "core__codec_base.sep.0.h"
/* method codec_base$Codec$char_max_size for (self: Codec): Int */
long core___core__Codec___char_max_size(val* self) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$encode_char for (self: Codec, Char): CString */
char* core___core__Codec___encode_char(val* self, uint32_t p0) {
char* var /* : CString */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$add_char_to for (self: Codec, Char, CString): Int */
long core___core__Codec___add_char_to(val* self, uint32_t p0, char* p1) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$encode_string for (self: Codec, Text): Bytes */
val* core___core__Codec___encode_string(val* self, val* p0) {
val* var /* : Bytes */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$add_string_to for (self: Codec, Text, Bytes): Int */
long core___core__Codec___add_string_to(val* self, val* p0, val* p1) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$codet_size for (self: Codec): Int */
long core___core__Codec___codet_size(val* self) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$max_lookahead for (self: Codec): Int */
long core___core__Codec___max_lookahead(val* self) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$is_valid_char for (self: Codec, CString, Int): Int */
long core___core__Codec___is_valid_char(val* self, char* p0, long p1) {
long var /* : Int */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$decode_char for (self: Codec, CString): Char */
uint32_t core___core__Codec___decode_char(val* self, char* p0) {
uint32_t var /* : Char */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
/* method codec_base$Codec$decode_string for (self: Codec, CString, Int): String */
val* core___core__Codec___decode_string(val* self, char* p0, long p1) {
val* var /* : String */;
if(catchStack.cursor >= 0){
longjmp(catchStack.envs[catchStack.cursor], 1);
}
PRINT_ERROR("Runtime error: %s", "FATAL: Dead method executed.");
PRINT_ERROR("\n");
fatal_exit(1);
RET_LABEL:;
return var;
}
