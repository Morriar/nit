# A package for parsing, handling, and generating email messages.
module utils

import builtins

redef class Sys
    
end

class LazyImporter
    
    fun getattr(name: Object) is abstract
    
    init do end
    
end

