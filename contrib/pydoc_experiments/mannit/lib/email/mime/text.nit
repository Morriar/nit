# A package for parsing, handling, and generating email messages.
module text

import iterators
import base64mime
import private__parseaddr
import utils
import sys
import quoprimime
import encoders
import charset
import errors
import mime
import message
import builtins

redef class Sys
    
end

redef class LazyImporter
    
    redef fun getattr(name) is abstract
    
    init do end
    
end

